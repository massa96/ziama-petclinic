server:
  port: 8761

eureka:
  instance:
    hostname: discovery-server
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      # Les clients iront sur le service Kubernetes discovery-server
      defaultZone: http://discovery-server:8761/eureka/
